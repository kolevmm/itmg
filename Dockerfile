# Използване на официалния Python образ
FROM python:3.9

# Задаване на работната директория в контейнера
WORKDIR /app

# Копиране на текущата директория в контейнера
COPY . /app

# Инсталиране на зависимостите
RUN pip install -r requirements.txt

# Излагане на порта, на който Flask ще работи
EXPOSE 5000

# Старт на приложението
CMD ["python", "app.py"]
